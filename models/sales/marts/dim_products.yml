models:
  - name: dim_products
    description: "Reference dimension table containing product catalog information"
    columns:
      - name: product_sk
        description: "Unique surrogate key identifier for product records"
        tests:
          - unique
          - not_null

      - name: product_id
        description: "Original business identifier for product records"
        tests:
          - unique
          - not_null
          
      - name: product_category_id
        description: "Business identifier linking to product category classification"

      - name: product_subcategory_id
        description: "Business identifier linking to product subcategory classification"

      - name: product_name
        description: "Official product name and description"
        tests:
          - not_null

      - name: product_category_name
        description: "Descriptive name of the product category"

      - name: product_subcategory_name
        description: "Descriptive name of the product subcategory"